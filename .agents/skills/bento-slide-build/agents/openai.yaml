interface:
  display_name: "Bento Slide Build"
  short_description: "Build deterministic Bento decks from HTML or legacy JSON"
  default_prompt: "Use $bento-slide-build to build this HTML-first Bento deck, validate native and fallback mappings, and report the evidence bundle."
