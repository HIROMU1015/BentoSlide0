interface:
  display_name: "Bento Slide Build"
  short_description: "Manage and build local HTML-first Bento deck workflows"
  default_prompt: "Use $bento-slide-build to inspect deck.yaml, advance the local workflow safely, build or finalize the Bento deck, and verify the required evidence."
