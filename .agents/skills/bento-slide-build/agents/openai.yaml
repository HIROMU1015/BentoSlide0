interface:
  display_name: "Bento Slide Build"
  short_description: "Manage, build, and safely fast-edit Bento decks"
  default_prompt: "Use $bento-slide-build to inspect deck.yaml, advance the workflow safely, batch final presentation edits, and verify the Bento deck."
